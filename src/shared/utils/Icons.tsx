import { FaLaptopCode, FaMapMarkedAlt, FaReact, FaServer, FaWordpress } from 'react-icons/fa';

export const iconsMap: Record<string, React.ElementType> = {
  FaLaptopCode,
  FaReact,
  FaWordpress,
  FaServer,
  FaMapMarkedAlt,
};
